@page "/przelew"
@inject NavigationManager NavigationManager

<h3>Przelew</h3>

<style>
    .valid.modified:not([type=checkbox]) {
        outline: 1px solid #26b050;
    }

    .invalid {
        outline: 1px solid red;
    }

    .validation-message {
        color: red;
    }
</style>

<EditForm Model="przelewDto" OnValidSubmit="HandlePrzelew">
    <DataAnnotationsValidator />
    <ValidationSummary />

        <div class="form-group">
            <p>Email:</p>
    <InputText  @bind-Value="@przelewDto.KontoOdbiorcy" class="form-control"></InputText>
        </div>
        <div class="form-group">
            <p>Password:</p>
    <InputNumber @bind-Value="przelewDto.Kwota"   class="form-control" HtmlAttributes="@htmlattribute" id="kwota"></InputNumber>
        </div>
    
   
        <button type="submit">Dokonaj przelewu</button>
       
</EditForm>




<h3> @przelewDto.Kwota</h3>
@code {
    private PrzelewDto przelewDto = new PrzelewDto();


    private async Task HandlePrzelew()
    {
        
    }

    Dictionary<string, object> htmlattribute = new Dictionary<string, object>()
    {
           { "maxlength", "5" },
    };

}
